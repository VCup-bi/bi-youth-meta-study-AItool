# bi-youth-meta-study

Screening tool for a systematic literature review on mental health outcomes among bisexual/bi+ youth. Uses GPT-4o to assess references in RIS format based on predefined inclusion and exclusion criteria.

## Gebruik

```bash
pip install -r requirements.txt
export OPENAI_API_KEY=your_key
python main.py
```
